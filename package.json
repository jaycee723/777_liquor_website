{
  "name": "777-liquor-website-cloudflare",
  "private": true,
  "scripts": {
    "build": "npm install && cd 777-Liquor-Website/website && bun install && bun run build",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
